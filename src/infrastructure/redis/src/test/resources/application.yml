event:
    stream-key: event-stream
    consumer-group: kakaotalk
    batch-size: 1000000
    pending-messages:
        check-interval: 0/1 * * ? * *
        timeout: 60000

spring:
    data:
        redis:
            host: ${REDIS_HOST}
            port: ${REDIS_PORT}

